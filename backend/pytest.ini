[pytest]
testpaths = tests
pythonpath = .
addopts =
    -ra
    --cov=.
    --cov-config=.coveragerc
    # Full-suite line+branch coverage (raise toward 90% as support_notify / email_service gain tests)
    --cov-fail-under=72
