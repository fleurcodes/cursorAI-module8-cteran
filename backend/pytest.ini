[pytest]
testpaths = tests
pythonpath = .
addopts =
    -ra
    --cov=.
    --cov-config=.coveragerc
    # Full-suite line+branch coverage (raise toward 90% as remaining branches shrink)
    --cov-fail-under=76
