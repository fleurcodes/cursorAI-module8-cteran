[pytest]
testpaths = tests
pythonpath = .
addopts =
    -ra
    --cov=.
    --cov-config=.coveragerc
    # Full-suite line+branch coverage (raise toward 90% as support_tickets / services gain tests)
    --cov-fail-under=66
